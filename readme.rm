 // si quiere conectarse  con sql server cambie el mentodo en la clase  main me.sqlServe
CREATE TABLE registro(
 ID int(11) PRIMARY KEY AUTO_INCREMENT,
    DNI int(11),
    Nombre varchar(50),
    Apellido varchar(50),
    Nacionalidad varchar(50)
)